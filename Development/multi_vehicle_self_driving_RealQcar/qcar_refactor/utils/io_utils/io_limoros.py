from core import BaseVehicleIO

class LimoIO(BaseVehicleIO):
    # No qcar object — publishes to /cmd_vel or similar
    # Different sensor sources
    # steering may not apply (differential drive)
    pass