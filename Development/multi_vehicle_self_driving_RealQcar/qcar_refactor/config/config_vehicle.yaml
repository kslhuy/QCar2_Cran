vehicle:
  car_id: 0

runtime:
  loop_rate_hz: 100

modules:
  model: example_vehicle
  io: qcar
  observer: ekf_qcar
  planner: static_default
  controller: simple_qcar
  v2v: udp_default
  simulation: "null"
  ground_station: "null"

mission:
  # Physical deployment starts route-free. Select an approved path explicitly
  # through the ground station or a dedicated operational vehicle profile.
  path: null
  target_velocity: 0.6
